# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$*.py

# PyTorch models / weight files
*.pt
*.bin
*.h5

# Local IDEs / System files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Excel temporary / owner files
~$*.xlsx
~$*.xls

# Ignore the nested MEDHA copy if it's redundant (we can ask or just ignore cache/models globally)
# (For now let's apply the rule globally, which will ignore *.pt and __pycache__ in both directories)

# Environments & Virtual Environments
.env
.venv/
venv/
env/

